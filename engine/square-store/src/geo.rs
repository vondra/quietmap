//! Flat-earth geometry — re-exported from [`grid::geo`], the single home.
//!
//! Duplicated here only until the `noise-compute` transfer reunites both
//! (this module then stays a re-export).

pub use grid::geo::*;
